import os
from app import app
# from app.utils.schedules import scheduler


if __name__ == '__main__':
    """WIP: Para la verificación de mensajes rescatados"""
    # scheduler.init_app(app)
    # scheduler.start()
    app.run()
